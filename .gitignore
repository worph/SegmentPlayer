# Cache
__pycache__/
*.pyc
.cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
.docker/

# Local testing
docker-compose.local.yml

# Local media files (keep media/README.md)
media/*
!media/README.md

# Vendor libs are bundled into the repo so the Docker image is CDN-independent.
# (The custom sp-audio libav.js variant — see docs/libav-custom-build.md — is
# not available on any CDN, so we commit everything together.)
CLAUDE.md
.claude/settings.local.json
benchmarks/tomato.md
.build-tmp
.claude
